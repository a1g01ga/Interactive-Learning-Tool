# Example environment variables for the Interactive Learning Tool
# Copy this file to `.env` and fill in the values. The application uses python-dotenv to load them.

# Required
OPENAI_API_KEY=your_openai_api_key_here

# Optional (defaults to gpt-4o if not set)
OPENAI_MODEL=gpt-4o
